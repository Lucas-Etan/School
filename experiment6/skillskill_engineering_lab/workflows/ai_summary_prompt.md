请根据下面的工作流记录，帮我总结一个可复用的 Skill 草稿。

要求：

1. 输出 `SKILL.md` 格式，包含 YAML frontmatter。
2. `name` 使用小写英文和连字符。
3. `description` 写清楚何时应该触发这个 Skill。
4. 正文包含适用场景、不适用场景、输入要求、工作流程、输出要求、自查清单和失败处理。
5. 不要写入真实 API Key、个人隐私、绝对路径或无法验证的结论。
6. 不要把一次性细节误写成通用规则。
7. 如果某些步骤依赖特定平台，请标注“以课程环境为准”。

下面是工作流记录：

...